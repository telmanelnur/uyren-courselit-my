// Minimal local AI-related type aliases to avoid depending on @tiptap-pro/extension-ai
// Extend these as needed to match your usage

export type Language = string
export type Tone = string
export type TextOptions = Record<string, unknown>

